<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for agency in agencies %}
        <url>
            <loc>{{ url('front_agency_details',{slug:agency.slug}) }}</loc>
        </url>
    {% endfor %}
</urlset>